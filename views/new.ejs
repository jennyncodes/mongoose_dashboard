<!DOCTYPE html>
<html>
<head>
	<title>New Dog</title>
</head>
<body>
	<fieldset>
		<legend>Add a New Dog</legend>
		<form method="post" action="/dogs/new">
			<label for="name">Name:</label>
			<input type="text" name="name">
			<label for="age">Age: </label>
			<input type="text" name="age">
			<input type="submit" name="submit" value="Create Dog">
		</form>
	</fieldset>
	<% if(typeof(errors) != 'undefined'){%>
			<% for(var e in errors){ %>
				<p><%=errors[e].message %></p>
			<% } %>
		<% } %>
	</div>
</body>
</html>
